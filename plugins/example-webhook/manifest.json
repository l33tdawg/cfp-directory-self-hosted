{
  "name": "example-webhook",
  "displayName": "Webhook Notifications",
  "version": "1.0.0",
  "apiVersion": "1.0",
  "description": "Sends webhook notifications when submission and review events occur. A reference plugin demonstrating hooks, configuration, and background jobs.",
  "author": "CFP Directory",
  "homepage": "https://github.com/l33tdawg/cfp-directory-self-hosted",
  "permissions": [
    "submissions:read"
  ],
  "hooks": [
    "submission.created",
    "submission.statusChanged",
    "review.submitted"
  ],
  "configSchema": {
    "type": "object",
    "properties": {
      "webhookUrl": {
        "type": "string",
        "title": "Webhook URL",
        "description": "The URL to send webhook payloads to (must be HTTPS)"
      },
      "secret": {
        "type": "string",
        "title": "Webhook Secret",
        "description": "Shared secret for HMAC signature verification",
        "format": "password"
      },
      "events": {
        "type": "array",
        "title": "Enabled Events",
        "description": "Which events to send webhooks for",
        "items": {
          "type": "string"
        }
      },
      "retryOnFailure": {
        "type": "boolean",
        "title": "Retry on Failure",
        "description": "Retry webhook delivery via background jobs if the initial request fails",
        "default": true
      }
    },
    "required": ["webhookUrl"]
  }
}
